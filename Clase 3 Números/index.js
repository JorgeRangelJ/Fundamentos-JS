// Definir una variable
var edad = 23;

// Incremento a una variable
edad += 1;

var peso = 65;
// Decremento a una variable
peso -= 2;

var sandwich = 1;
peso += sandwich;

var jugarFutbol = 3;

peso -= jugarFutbol;

// Decimales

var preciovino = 200.3;

var total = Math.round(preciovino * 100 * 3) / 100;
var totalStr = total.toFixed(2);
var totalNumero = parseFloat(totalStr);

var pizza = 8;
var persona = 2;

var cantidadDePorcionesPorPersona = pizza / persona;